

## Changes

1. **Update `src/pages/Pricing.tsx`**: Center-align the "Total Applicants per Year" label and value by changing the `flex justify-between` layout to a centered column layout with `text-center`.

